@model IEnumerable<Client_Control_001.Models.Compras>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cod_compra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cod_cli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Empresas.Cod_empresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_compra)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_compra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_cli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empresas.Cod_empresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_compra)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new {  id=item.Cod_compra  }) |
            @Html.ActionLink("Detailes", "Details", new {/* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Eliminar", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
